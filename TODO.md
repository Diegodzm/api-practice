# TODO List for Adding POST Button

- [x] Edit `index.html` to add a new button `<button id="postButton">Post Data</button>` next to the existing "Fetch Data" button.
- [x] Edit `app.js` to update the `postData` function to perform the specified POST request, parse the JSON response, and display it in the output box.
- [x] Add an event listener to the new button in `app.js` to call `postData` on click.
- [x] Test the functionality by opening `index.html` in a browser to ensure the POST response appears in the output box.
